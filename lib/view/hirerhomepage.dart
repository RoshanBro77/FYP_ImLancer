import 'package:flutter/material.dart';

class HirerHomePage extends StatelessWidget {
  const HirerHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
